'use strict';

/**
 * sub-module-progress service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService(
  'api::sub-module-progress.sub-module-progress'
);
