'use strict';

/**
 * sub-module-progress router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::sub-module-progress.sub-module-progress');
