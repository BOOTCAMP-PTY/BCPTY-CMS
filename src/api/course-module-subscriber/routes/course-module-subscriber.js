'use strict';

/**
 * course-module-subscriber router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter(
  'api::course-module-subscriber.course-module-subscriber'
);
