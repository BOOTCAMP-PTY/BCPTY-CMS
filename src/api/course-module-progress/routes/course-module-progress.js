'use strict';

/**
 * course-module-progress router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter(
  'api::course-module-progress.course-module-progress'
);
